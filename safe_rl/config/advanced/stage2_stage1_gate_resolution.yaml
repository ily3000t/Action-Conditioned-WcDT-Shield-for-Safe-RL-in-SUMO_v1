# Stage2-only Stage1 main-gate resolution boost profile.
# Keep default pipeline behavior unchanged; only enable Stage1 source resolution regularization.

world_model:
  pair_ft_stage1_resolution_loss_weight: 0.01
  pair_ft_stage1_resolution_min_score_gap: 0.015

tensorboard:
  run_name: "stage2_stage1_gate_resolution"
