# Stage1-driven recovery override (gap=0.008).
# Thin profile: keep prior Stage1 recovery coverage and only relax probe min gap.

stage1_collection:
  probe_max_steps_per_episode: 6
  probe_pair_boundary_keep_per_risky_step: 2
  probe_pair_min_target_gap: 0.008

tensorboard:
  run_name: "stage1_probe_recovery_gap008"
