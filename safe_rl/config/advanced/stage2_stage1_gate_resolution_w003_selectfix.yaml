# Stage2-only Stage1 main-gate resolution + selection tie-epsilon diagnostic profile (7A2).
# Single-variable step over w002_selectfix: increase Stage1-resolution weight from 0.02 to 0.03.

world_model:
  pair_ft_stage1_resolution_loss_weight: 0.03
  pair_ft_stage1_resolution_min_score_gap: 0.015
  pair_ft_selection_accuracy_tie_epsilon: 0.01

tensorboard:
  run_name: "stage2_stage1_gate_resolution_w003_selectfix"
