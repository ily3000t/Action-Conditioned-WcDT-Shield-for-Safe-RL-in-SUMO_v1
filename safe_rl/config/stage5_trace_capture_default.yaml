# Stage5 trace-capture profile (default behavior).
# Purpose: produce lightweight, isolated trace artifacts for visualization/anomaly diagnosis.

shield_trace:
  enabled: true
  save_pair_traces: true
  trace_dir_name: "stage5_trace_capture_default"
  seed_list: [42, 123, 2024, 7, 11, 29, 47, 64]

eval:
  eval_episodes: 8

tensorboard:
  run_name: "stage5_trace_capture_default"
