# Stage2 v2 specialist override.
# This file intentionally stays thin and inherits all other fields from default_safe_rl.yaml.

light_risk:
  pair_finetune: false

world_model:
  pair_finetune: true
  stage5_pair_weight: 1.0
  stage4_pair_weight: 0.2

tensorboard:
  run_name: "stage2_v2_world_pair_focus"
